// import { UserRegistrationInput } from "../entities/profile/UserRegistrationInput";

// export const validateRegistration = (options: UserRegistrationInput) => {
//   //TODO: HANDLE VALIDATION ON TYPEORM LEVEL
//   // TODO: add additional validation
//   // if (options.username.length <= 2) {
//   //   return errorMessage("username", "username length must be greater than 2");
//   // }

//   // if (options.username.includes("@")) {
//   //   return errorMessage("username", "username cannot contain an @");
//   // }

//   // if (options.password.length <= 3) {
//   //   return errorMessage("password", "password length must be greater than 3");
//   // }

//   return null;
// };

// function errorMessage(field: string, message: string) {
//   return [{ field, message }];
// }
